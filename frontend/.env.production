# Production environment variables
# These will be automatically set by the smart fallbacks in the code
# But you can override them if needed

# REACT_APP_API_URL will default to window.location.origin
# REACT_APP_WS_URL will default to wss://your-domain.railway.app
